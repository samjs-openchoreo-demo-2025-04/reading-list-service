apiVersion: score.dev/v1b1
metadata:
  name: reading-list-service
  project: default-project
  organization: reading-list-service
service:
  ports:
    default:
      port: 8080
      protocol: TCP
containers:
  container-one1:
    image: .